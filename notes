command
parse command --> cmd args //ft_strchr
if cmd in builtins
	builtin(argv, env);
else
	extern(argv, env);

extern(char **argv, char **env)
	search for the command in path
	execve in another process

---------------------------

echo
	print args or env var

cd
	access
	chdir
	env pwd

setenv
	change env var in char **env

unsetenv
	remove env var in char **env

env
	chang env
	call extern
----------------------------
